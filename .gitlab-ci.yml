stages:
  - test
  - build
  - deploy

include:
  - project: hydra/dev/ci-templates
    file: /v1/static/publish.yml

test:
  stage: test
  tags:
    - shared
  script:
    - npm install
    - npm test

build publish:
  variables:
    GIT_STRATEGY: none
  script: echo job skipped
